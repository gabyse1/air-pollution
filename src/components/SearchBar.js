const SearchBar = () => (
  <section className="page__section" id="section__searchbar">
    <input type="search" name="input__search-location" id="input__search-location" placeholder="Input location's name" />
  </section>
);

export default SearchBar;
